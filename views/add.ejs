<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add New Movie | MovieVerse</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a, #1a1a1a);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
    }

    /* Navbar */
    .navbar {
      background: rgba(0, 0, 0, 0.85);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 70, 70, 0.3);
    }

    .navbar-brand {
      font-weight: 700;
      font-size: 1.6rem;
      color: #ff4646 !important;
      text-shadow: 0 0 8px rgba(255, 70, 70, 0.7);
    }

    .btn-outline-light {
      border-color: #ff4646;
      color: #ff4646;
    }

    .btn-outline-light:hover {
      background-color: #ff4646;
      color: #fff;
      box-shadow: 0 0 10px #ff4646;
    }
    .form-container {
      background: rgba(25, 25, 25, 0.95);
      border-radius: 15px;
      padding: 40px;
      box-shadow: 0 0 25px rgba(255, 70, 70, 0.2);
    }

    h2 {
      color: #ff4646;
      font-weight: 600;
      letter-spacing: 1px;
      border-bottom: 2px solid #ff4646;
      display: inline-block;
      padding-bottom: 5px;
    }

    .form-label {
      color: #ff8c8c;
      font-weight: 600;
    }

    .form-control {
      background-color: #121212;
      color: #fff;
      border: 1px solid #333;
    }

    .form-control:focus {
      border-color: #ff4646;
      box-shadow: 0 0 5px #ff4646;
    }

    .btn-primary {
      background-color: #ff4646;
      border: none;
      transition: 0.3s;
    }

    .btn-primary:hover {
      background-color: #ff5f5f;
      box-shadow: 0 0 10px #ff4646;
    }

    .btn-secondary {
      background-color: #333;
      border: none;
    }

    .btn-secondary:hover {
      background-color: #555;
    }

    footer {
      text-align: center;
      color: #777;
      padding: 20px;
      border-top: 1px solid #333;
      margin-top: 40px;
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/movie/get"> MovieVerse</a>
      <div>
        <a href="/movie/add" class="btn btn-outline-light btn-sm"> Add Movie</a>
      </div>
    </div>
  </nav>

  <!-- Main Form -->
  <div class="container mt-5">
    <div class="form-container mx-auto col-md-8">
      <h2 class="mb-4 text-center">Add New Movie</h2>

      <form action="/movie/add" method="POST" enctype="multipart/form-data">
        <!-- Title -->
        <div class="mb-3">
          <label class="form-label">Title</label>
          <input type="text" name="title" class="form-control" placeholder="Enter movie title" required>
        </div>

        <!-- Genre -->
        <div class="mb-3">
          <label class="form-label">Genre</label>
          <input type="text" name="genere" class="form-control" placeholder="Enter genre (e.g. Action, Comedy)" required>
        </div>

        <!-- Year -->
        <div class="mb-3">
          <label class="form-label"> Year</label>
          <input type="number" name="year" class="form-control" placeholder="Enter release year" required>
        </div>

        <!-- Rating -->
        <div class="mb-3">
          <label class="form-label"> Rating</label>
          <input type="number" step="0.1" name="rating" class="form-control" placeholder="Enter rating (e.g. 8.5)" required>
        </div>

        <!-- Image -->
        <div class="mb-3">
          <label class="form-label"> Poster</label>
          <input type="file" name="image" class="form-control">
        </div>

        <!-- Buttons -->
        <div class="d-flex justify-content-between">
          <button type="submit" class="btn btn-primary px-4">Add Movie</button>
          <a href="/movie/get" class="btn btn-secondary px-4">Back</a>
        </div>
      </form>
    </div>
  </div>

  <footer>
    <p>© <%= new Date().getFullYear() %> MovieVerse. All Rights Reserved.</p>
  </footer>

</body>
</html>
